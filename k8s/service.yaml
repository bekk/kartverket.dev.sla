apiVersion: v1
kind: Service
metadata:
  name: service-backstage
  namespace: ns-backstage
  labels:
    app.kubernetes.io/name: spire-backstage
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: spire-backstage
  ports:
    - name: http
      port: 80
      targetPort: http